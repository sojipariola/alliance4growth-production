# Operations

- `backup.sh` creates a SQLite-consistent database backup and an uploads archive.
- Keep `/var/backups/alliance4growth` on storage separate from the application server when possible.
- Test restores periodically; a backup that has never been restored is not a proven backup.
